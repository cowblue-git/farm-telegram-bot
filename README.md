# Farm Telegram Bot

**Telegram-бот фермы «Голубой Коровы»**
Cloudflare Workers · KV Storage · Production-ready

## 📌 Описание

Этот репозиторий содержит **стабильную production-версию Telegram-бота** для фермы «Голубой Коровы».

Бот предназначен для:

* записи на экскурсии;
* записи на новогодние мероприятия с контролем вместимости;
* хранения заявок в Cloudflare KV;
* уведомления администратора;
* подтверждения / отклонения заявок через inline-кнопки;
* просмотра сводки мероприятий администратором.

Текущая версия: **v1.0 (stable)**.

---

## ⚙️ Архитектура

* **Cloudflare Workers** — серверная логика
* **Telegram Bot API** — взаимодействие с пользователями
* **Cloudflare KV**:

  * `STATE` — состояния пользователей (диалоги)
  * `BOOKINGS` — все заявки
  * `EVENTS` — состояние мероприятий (вместимость / занято)
* Webhook: `/webhook`
* Динамические сообщения отправляются **без Markdown**, чтобы избежать ошибок парсинга

---

## 🧱 Основные возможности

### Пользователь

* Главное меню с кнопками
* Запись на экскурсию
* Запись на новогодние мероприятия
* Пошаговый сбор данных (имя → дата → время → гости → контакт)
* Получение подтверждений и уведомлений

### Администратор

* Получение всех заявок в админ-чат
* Inline-кнопки:

  * ✅ Подтвердить заявку
  * ❌ Отклонить заявку
* Просмотр сводки по мероприятиям
* Просмотр заявок по конкретному мероприятию

Админ-команды:

* `/events` — меню мероприятий
* `/dump_bookings [фильтр]` — выгрузка заявок из KV
* `/confirm_event <eventId>` — массовое подтверждение заявок

---

## 🔐 Переменные окружения (Bindings)

### Обязательные

| Переменная      | Назначение                             |
| --------------- | -------------------------------------- |
| `BOT_TOKEN`     | Токен Telegram-бота                    |
| `ADMIN_CHAT_ID` | Chat ID, куда отправляются уведомления |
| `ADMIN_USER_ID` | user_id администратора (права)         |

### KV Namespaces

| Namespace  | Назначение                |
| ---------- | ------------------------- |
| `STATE`    | Состояния пользователей   |
| `BOOKINGS` | Все заявки                |
| `EVENTS`   | Мероприятия и вместимость |

---

## 🚀 Деплой

1. Создать Cloudflare Worker
2. Добавить KV namespaces и environment variables
3. Указать маршрут `/webhook`
4. Установить webhook Telegram:

```
https://api.telegram.org/bot<BOT_TOKEN>/setWebhook?url=https://<worker>.workers.dev/webhook
```

---

## 🧪 Проверка

```
https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo
```

Убедитесь, что:

* URL корректный
* `last_error_message` отсутствует

---

## 🗂 Структура проекта

```
.
├── worker.js        # Cloudflare Worker (основная логика)
├── README.md        # Документация
├── CHANGELOG.md     # История изменений
└── .gitignore
```

---

## 🏷 Версии

* **v1.0** — стабильный production-релиз

---

## 📄 Статус

✅ Используется в продакшене
✅ Протестирован на реальных заявках
✅ Готов к дальнейшему развитию
